---
name: TypeScript Guidelines
description: Type safety, strict typing, no `any`, generics, type guards for the PI-RAT app
globs:
  - '**/*.ts'
  - '**/*.tsx'
alwaysApply: true
---

# TypeScript Guidelines

## Public API

- All exports have explicit types (params + return).
- Generic params have constraints + descriptive names (`TUser`, `TArgs` — not `T`, `R`).
- Prefer `type` over `interface`. No `I` prefix, no `Type` suffix.

```typescript
// ✅
export const UsersService: Token<UsersService, 'singleton'> = defineService({
  name: 'pi-rat/UsersService',
  lifetime: 'singleton',
  factory: ({ inject }): UsersService => new UsersServiceImpl(inject(IdentityApiClient)),
})

// ❌ implicit return type, single-letter generic
export function fetchData<T>(url) { return fetch(url) }
```

## NO `any` — one exception

`any` forbidden in values, params, return types. **Exception:** generic constraint position when variance breaks `unknown` inference (callback param positions).

```typescript
// ✅ unknown for truly unknown
const process = (data: unknown): string =>
  typeof data === 'string' ? data : JSON.stringify(data)

// ✅ any only allowed in constraint position
type ExtractPaths<T extends Record<string, NestedRoute<any>>> = { /* ... */ }

// ❌ any anywhere else
export function process(data: any): string { return data.toString() }
```

`@typescript-eslint/no-explicit-any` is **disabled** in `eslint.config.js` because the rule has no option for the constraint-position exception. Reviewers enforce the rule in code review.

## Inference

Let TS infer internally. Be explicit at API boundaries, complex switch/case, or when inference is ambiguous.

```typescript
// ✅ inferred internally
const users = ['Alice', 'Bob']
const count = users.length

// ✅ explicit at exports
export const getUserById = async (id: string): Promise<User | null> => { /* ... */ }

// ❌ noisy
const count: number = 5
```

## API contract types

Frontend and service share contracts through `common/src/apis/<module>.ts`. Each module exports a `RestApi`-derived interface. Always import the typed module API from `common`.

```typescript
import type { IdentityApi } from 'common'

// API client token (frontend)
export const IdentityApiClient: Token<IdentityApiClient, 'singleton'> = defineService({
  name: 'pi-rat/IdentityApiClient',
  lifetime: 'singleton',
  factory: () => new IdentityApiClientImpl(/* typed via IdentityApi */),
})
```

## Type guards

Use guards for runtime checks. Use them, not type assertions.

```typescript
// ✅
export const isUser = (value: unknown): value is User =>
  typeof value === 'object' && value !== null && 'username' in value

const isNonNullable = <T>(value: T | null | undefined): value is T =>
  value !== null && value !== undefined

users.filter(isNonNullable) // type-narrowed
```

## Discriminated unions

For multi-state values. Always include a `status` / `kind` discriminator.

```typescript
// ✅
type CacheState<T> =
  | { status: 'idle' }
  | { status: 'loading' }
  | { status: 'loaded'; value: T }
  | { status: 'error'; error: string }

// ❌ no discriminator → cannot narrow
type Bad<T> = { loading: boolean; error?: string; data?: T }
```

## Observable typing

`ObservableValue<T>` is invariant — provide explicit `T`, especially for nullable / union states.

```typescript
// ✅
public currentUser = new ObservableValue<User | null>(null)
public state = new ObservableValue<SessionState>('initializing')

// ❌ inference widens to ObservableValue<null>
public currentUser = new ObservableValue(null)
```

## Assertions

Avoid. Use type guards or `as const` instead.

```typescript
// ✅ as const for literal types
const colors = ['red', 'green', 'blue'] as const
type Color = (typeof colors)[number]

// ✅ assertion only when DOM API leaves no choice
const el = document.getElementById('x') as HTMLInputElement | null

// ❌ dangerous
const user = unknownValue as User       // prefer guard
const x = maybe!                        // prefer optional chaining
```

Avoid `!` non-null assertion. Prefer `?.` and `??`.

## Double-cast anti-pattern

Never `as unknown as T` (e.g. `undefined as unknown as MovieFile`). If you need a cast that strong, you're missing a type guard or a generic — refactor. For provider/adapter chains where partial results are valid, use a **narrow internal result type** (see `BACKEND_PATTERNS.mdc`).

## Utility types

Reuse built-ins (`Partial`, `Pick`, `Omit`, `Record`, `Required`, `Readonly`). Export project-specific helpers when reused 2+ times.

## Checklist (public API)

- [ ] No `any` (except generic constraint exception)
- [ ] Explicit types on all exports
- [ ] Generic params named & constrained
- [ ] Type guards for runtime checks
- [ ] Discriminated unions for multi-state
- [ ] No `as unknown as T`, no `!` non-null
- [ ] `ObservableValue` types explicit

Tools: `yarn build` runs the actual build (do **not** run `tsc -b` directly per `SCRIPT_EXECUTION.mdc`).
