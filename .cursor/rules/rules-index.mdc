---
alwaysApply: true
---

Index of repository rules. Read the linked file when its topic is relevant to the user's prompt.

- [Naming, formatting, imports, file/code organization](./CODE_STYLE.mdc) — auto-applied to all `.ts` / `.tsx`
- [Type safety, no `any`, generics, type guards, discriminated unions](./TYPESCRIPT_GUIDELINES.mdc) — auto-applied to all `.ts` / `.tsx`
- [Complexity thresholds for Shade components, services, REST actions](./COMPLEXITY.mdc) — auto-applied; used in reviews and during implementation
- [Backend REST patterns — actions, auth, errors, services, provider chains](./BACKEND_PATTERNS.mdc) — auto-applied in `service/`
- [Frontend Shade patterns — service-cached data, routing, components, forms, UI entry points](./FRONTEND_PATTERNS.mdc) — auto-applied in `frontend/`
- [Observable state — ObservableValue, useObservable / useDisposable, subscriptions, perf](./OBSERVABLE_STATE.mdc) — auto-applied in `frontend/` and `service/`
- [Async patterns — fire-and-forget catch, fs/promises, singleton concurrency, dispose-before-reinit](./ASYNC_PATTERNS.mdc) — auto-applied in `frontend/` and `service/`
- [FuryStack Cache patterns — capacity, getObservable, invalidation](./CACHE_HANDLING.mdc) — auto-applied in `frontend/`
- [Error handling — RequestError, observable errors, retry, graceful degradation](./ERROR_HANDLING.mdc) — auto-applied in `service/` and `frontend/`
- [REST action `Validate()` wrappers + explicit param checks before system calls](./REST_ACTION_VALIDATION.mdc) — auto-applied in `service/**/actions/`
- [Vitest unit + Playwright E2E patterns](./TESTING_GUIDELINES.mdc) — auto-applied to specs
- [Yarn workspace commands; never run `tsc` directly](./SCRIPT_EXECUTION.mdc) — auto-applied to `package.json` and shell scripts
- [Runtime MFE type contracts (host ↔ monaco-mfe)](./MFE_TYPE_CONTRACTS.mdc) — auto-applied in `frontend/` and `monaco-mfe/`

Skills (on-demand workflows):

- `create-api-endpoint` — add a REST endpoint end-to-end
- `setup-data-layer` — wire an entity (model + store + DataSet + entity sync)
- `create-shade-component` — author a Shade component with proper hooks, styling, tests
- `write-tests` — author Vitest unit / integration / Playwright E2E tests
- `fill-changelog` — fill changelog entries for the current branch
- `review-changes` — full code review with reviewer subagents
