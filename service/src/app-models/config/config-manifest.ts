import type { AppModelManifest } from 'common'

export const configManifest: AppModelManifest = {
  id: '@pi-rat/config',
  name: 'Configuration',
  description: 'Manages application configuration settings.',
  version: '1.0.0',
  integrations: [],
  apiRedirects: {},
  requiredPermissions: {},
}
